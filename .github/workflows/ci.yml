name: CI

on:
  pull_request:
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - 'renovate.json'
      - '.github/dependabot.yml'
      - '.github/workflows/docs.yml'
      - '.github/workflows/publish.yml'
      - '.github/workflows/release-drafter.yml'
      - '.github/workflows/pr-title.yml'
  push:
    branches: [main]
    paths-ignore:
      - '**.md'
      - 'docs/**'
      - 'LICENSE'
      - 'renovate.json'
      - '.github/dependabot.yml'
      - '.github/workflows/docs.yml'
      - '.github/workflows/publish.yml'
      - '.github/workflows/release-drafter.yml'
      - '.github/workflows/pr-title.yml'

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
      - run: uv sync --extra dev
      - run: uv run ruff check netbox_peering_manager
      - run: uv run ruff format --check netbox_peering_manager

  test:
    name: Test (Python ${{ matrix.python }}, NetBox ${{ matrix.netbox }})
    runs-on: ubuntu-latest
    needs: lint
    permissions:
      contents: read
      id-token: write
    strategy:
      fail-fast: false
      matrix:
        python: ["3.12", "3.13", "3.14"]
        netbox: ["4.5.10", "4.6.3"]
    services:
      postgres:
        image: postgres:18-alpine
        env:
          POSTGRES_DB: netbox
          POSTGRES_USER: netbox
          POSTGRES_PASSWORD: ci-password
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
      redis:
        image: redis:8-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - uses: actions/checkout@v7

      - uses: astral-sh/setup-uv@v10.0.1
        with:
          enable-cache: true
          python-version: ${{ matrix.python }}

      - name: Clone NetBox v${{ matrix.netbox }}
        run: |
          git clone --depth 1 --branch v${{ matrix.netbox }} \
            https://github.com/netbox-community/netbox.git /opt/netbox

      - name: Configure NetBox
        run: |
          cat > /opt/netbox/netbox/netbox/configuration.py << 'EOF_CFG'
          ALLOWED_HOSTS = ["*"]

          DATABASES = {
              "default": {
                  "ENGINE": "django.db.backends.postgresql",
                  "NAME": "netbox",
                  "USER": "netbox",
                  "PASSWORD": "ci-password",
                  "HOST": "localhost",
                  "PORT": "5432",
                  "CONN_MAX_AGE": 300,
              }
          }

          REDIS = {
              "tasks":   {"HOST": "localhost", "PORT": 6379, "PASSWORD": "", "DATABASE": 0, "SSL": False},
              "caching": {"HOST": "localhost", "PORT": 6379, "PASSWORD": "", "DATABASE": 1, "SSL": False},
          }

          SECRET_KEY = "ci-only-secret-key-do-not-use-in-production-aaaaaaaaaaaaaaaaaaaa"
          API_TOKEN_PEPPERS = {1: (SECRET_KEY * 2)[:50]}

          PLUGINS = ["netbox_routing", "netbox_peering_manager"]
          PLUGINS_CONFIG = {"netbox_peering_manager": {}}
          EOF_CFG

      - name: Install plugin and NetBox dependencies
        run: |
          uv venv
          uv pip install -r /opt/netbox/requirements.txt
          uv pip install -e ".[dev]"
          echo "$GITHUB_WORKSPACE/.venv/bin" >> "$GITHUB_PATH"
          echo "VIRTUAL_ENV=$GITHUB_WORKSPACE/.venv" >> "$GITHUB_ENV"

      - name: Run migrations
        env:
          PYTHONPATH: /opt/netbox/netbox
          DJANGO_SETTINGS_MODULE: netbox.settings
          NETBOX_CONFIGURATION: netbox.configuration
        run: |
          cd /opt/netbox/netbox
          python manage.py migrate --noinput

      - name: Run pytest with coverage
        env:
          PYTHONPATH: /opt/netbox/netbox
          DJANGO_SETTINGS_MODULE: netbox.settings
          NETBOX_CONFIGURATION: netbox.configuration
        run: |
          pytest netbox_peering_manager/tests/ -v --tb=short --cov=netbox_peering_manager --cov-report=xml

      - name: Upload coverage to Codecov
        if: matrix.python == '3.13' && matrix.netbox == '4.5.10'
        continue-on-error: true
        uses: codecov/codecov-action@v7
        with:
          use_oidc: true
          files: ./coverage.xml
          fail_ci_if_error: false

      - name: Check migrations
        env:
          PYTHONPATH: /opt/netbox/netbox
          DJANGO_SETTINGS_MODULE: netbox.settings
          NETBOX_CONFIGURATION: netbox.configuration
        run: |
          cd /opt/netbox/netbox
          python manage.py makemigrations netbox_peering_manager --check --dry-run

      - name: Django system check
        env:
          PYTHONPATH: /opt/netbox/netbox
          DJANGO_SETTINGS_MODULE: netbox.settings
          NETBOX_CONFIGURATION: netbox.configuration
        run: |
          cd /opt/netbox/netbox
          python manage.py check

  build:
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v10.0.1
      - run: uv build
      - uses: actions/upload-artifact@v7
        with:
          name: dist
          path: dist/
